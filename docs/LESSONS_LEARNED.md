# Уроки проекта: Проблемы и Решения

**Дата создания:** 2025-01-XX  
**Цель:** Зафиксировать основные проблемы, с которыми столкнулись при переверстке, и их решения для предотвращения повторения ошибок.

---

## 🔴 Критические проблемы

### 1. Изменение контента без согласования

**Проблема:**
- ИИ-агент изменял тексты, alt-атрибуты, title, meta description без предупреждения
- Пользователь обнаружил, что SEO-оптимизированные тексты были урезаны или изменены
- Это критично для сайта, где каждый текстовый элемент прорабатывался отдельно

**Примеры:**
- Meta description был урезан (удалена часть "более 700 примеров в 18 стилях")
- Alt-атрибуты изображений были сокращены (например, "Шарж по фото" вместо "Шарж по фото на Анатолия Вассермана")
- FAQ-ответы были изменены без согласования

**Решение:**
- ✅ Добавлено правило в `AI_INSTRUCTIONS.md`: **ЗАПРЕЩЕНО изменять контент без согласования**
- ✅ Правило: сначала СПРОСИТЬ, показать изменения, дождаться подтверждения
- ✅ Восстановлены все оригинальные тексты из `index.php`

**Что документировать:**
- ✅ **ДА** — правило о запрете изменения контента (уже в `AI_INSTRUCTIONS.md`)
- ✅ **ДА** — чек-лист перед сохранением страницы (проверка alt, title, текстов)

---

### 2. Неправильный источник компонентов

**Проблема:**
- ИИ-агент создавал новые header/footer вместо использования готовых из дизайн-системы
- Брал компоненты из неправильных мест (например, из упрощённой версии страницы)
- Это привело к несоответствию дизайна между страницами

**Решение:**
- ✅ Чётко указано в `AI_INSTRUCTIONS.md`: использовать компоненты из `src/components/`
- ✅ Создан каталог компонентов в `docs/COMPONENTS.md`
- ✅ Удалены дублирующие/неправильные версии компонентов

**Что документировать:**
- ✅ **ДА** — список компонентов и их расположение (уже в `COMPONENTS.md`)
- ✅ **ДА** — правило: всегда брать из дизайн-системы, не создавать новые

---

### 3. Проблемы с брейкпоинтами

**Проблема:**
- Попытка перейти с Bootstrap 3 breakpoints (768px, 992px, 1200px) на стандартные Tailwind (640px, 768px, 1024px)
- Верстка "поехала" — элементы сместились, кнопки исчезли
- Пришлось откатывать изменения

**Решение:**
- ✅ Решено оставить стандартные Tailwind breakpoints (sm=640, md=768, lg=1024, xl=1280, 2xl=1536)
- ✅ Обновлена документация: используются стандартные Tailwind breakpoints
- ✅ Проверка верстки после изменения breakpoints обязательна

**Что документировать:**
- ✅ **ДА** — какие breakpoints используются (уже в `AI_INSTRUCTIONS.md`)
- ⚠️ **НЕТ** — детали неудачной попытки миграции (не нужно, только факт: используем стандартные)

---

### 4. Проблемы с компиляцией Tailwind

**Проблема:**
- При попытке использовать "чистый Tailwind" классы не компилировались в `output.css`
- Страница выглядела сломанной (нет кнопок, неправильные цвета)
- Причина: `npm run build` не запускался автоматически

**Решение:**
- ✅ Для эталонной страницы (`portret-na-zakaz-po-foto-na-kholste-sankt-peterburg.html`) использован Tailwind CDN для разработки
- ✅ CDN включает все utility-классы по умолчанию, удобен для быстрой разработки
- ✅ Для продакшена: использовать локальную компиляцию через `npm run build` → `output.css`
- ✅ Все новые страницы должны создаваться на основе эталона, но использовать скомпилированный CSS

**Важно:**
- Эталонная страница использует CDN (`<script src="https://cdn.tailwindcss.com"></script>`) — это нормально для разработки
- При создании новых страниц на основе эталона заменить CDN на `<link rel="stylesheet" href="css/output.css">`
- Главная и печать нужно переделать: они были созданы не на основе эталона и используют другую структуру

**Что документировать:**
- ✅ **ДА** — разница между CDN и локальной компиляцией (можно добавить в `HOW_TO_CREATE_PAGE.md`)
- ✅ **ДА** — указать эталонную страницу и подход к созданию новых страниц
- ⚠️ **НЕТ** — детали проблемы с npm (техническая деталь, не нужна в основной документации)

---

### 5. Неполная реализация функций

**Проблема:**
- ИИ-агент утверждал, что работа завершена, но пользователь находил отсутствующие элементы:
  - FAQ был пустым
  - Карусели не работали на ПК
  - Видео не воспроизводилось
  - Преимуществ было 3 вместо 6
- Это приводило к потере времени и доверия

**Решение:**
- ✅ Добавлен чек-лист в `AI_INSTRUCTIONS.md` перед сохранением
- ✅ Правило: проверять все интерактивные элементы (карусели, видео, FAQ)
- ✅ Сравнивать количество элементов с оригиналом

**Что документировать:**
- ✅ **ДА** — чек-лист проверки перед сохранением (уже в `AI_INSTRUCTIONS.md`)
- ⚠️ **НЕТ** — список конкретных пропущенных элементов (это было разовое, не системная проблема)

---

### 6. Проблемы с пониманием задачи

**Проблема:**
- ИИ-агент иногда делал противоположное тому, что просил пользователь
- Пример: пользователь просил "продолжить работу с чистым Tailwind", а агент откатил изменения
- Это приводило к путанице и потере времени

**Решение:**
- ✅ Уточнение задачи перед началом работы
- ✅ Если непонятно — спросить пользователя

**Что документировать:**
- ✅ **ДА** — правило: "Если что-то непонятно — спроси пользователя" (уже в `AI_INSTRUCTIONS.md`)
- ⚠️ **НЕТ** — конкретные примеры недопонимания (не системная проблема)

---

## 🟡 Важные моменты (не критичные, но полезные)

### 7. Отсутствие атрибутов у изображений

**Проблема:**
- У некоторых изображений отсутствовали `decoding="async"`, `width`, `height`
- Это не критично, но влияет на производительность и CLS (Cumulative Layout Shift)

**Решение:**
- ✅ Добавлены недостающие атрибуты
- ✅ Правило: для всех изображений указывать `width`, `height`, `loading="lazy"`, `decoding="async"`

**Что документировать:**
- ✅ **ДА** — правило для изображений (можно добавить в `AI_INSTRUCTIONS.md` как рекомендацию)

---

### 8. SEO-элементы

**Проблема:**
- Отсутствовали некоторые SEO-элементы (og:image, canonical)
- Title страницы содержал "(Tailwind Test)" — не подходит для продакшена

**Решение:**
- ✅ Восстановлены оригинальные title и description
- ⚠️ og:image и canonical нужно добавить при интеграции с Bitrix

**Что документировать:**
- ✅ **ДА** — список обязательных SEO-элементов (можно добавить в `AI_INSTRUCTIONS.md`)

---

## ✅ Что УЖЕ задокументировано

1. ✅ Правило о запрете изменения контента (`AI_INSTRUCTIONS.md`)
2. ✅ Список компонентов и их расположение (`COMPONENTS.md`)
3. ✅ Breakpoints (`AI_INSTRUCTIONS.md`)
4. ✅ Чек-лист перед сохранением (`AI_INSTRUCTIONS.md`)
5. ✅ Правило "спросить, если непонятно" (`AI_INSTRUCTIONS.md`)

---

## 📝 Что НУЖНО добавить в документацию

### В `AI_INSTRUCTIONS.md`:

1. **Правило для изображений:**
```markdown
## Изображения

Для всех `<img>` указывать:
- `width` и `height` (реальные размеры изображения)
- `loading="lazy"` (кроме первого экрана)
- `decoding="async"` (опционально, но рекомендуется)
- `alt` (обязательно, из оригинала)
- `title` (если есть в оригинале)
```

2. **SEO-элементы:**
```markdown
## SEO-элементы (обязательные)

- `<title>` — без суффиксов типа "(Tailwind Test)"
- `<meta name="description">` — полный текст из оригинала
- `og:image` — добавить при интеграции с Bitrix
- `canonical` — добавить при интеграции с Bitrix
```

3. **Проверка перед сохранением:**
```markdown
## Проверка перед сохранением

- [ ] Все alt-атрибуты совпадают с оригиналом
- [ ] Все title-атрибуты совпадают с оригиналом
- [ ] Все тексты совпадают с оригиналом
- [ ] Количество элементов совпадает (преимуществ, FAQ-вопросов, примеров)
- [ ] Интерактивные элементы работают (карусели, видео, FAQ)
- [ ] Изображения имеют width/height
```

---

## ❌ Что НЕ нужно документировать

1. ❌ Детали технических ошибок (npm, компиляция) — это разовые проблемы
2. ❌ Конкретные примеры пропущенных элементов — это было разовое, не системное
3. ❌ Детали неудачной миграции breakpoints — важен только факт: используем стандартные
4. ❌ Примеры недопонимания задач — это не системная проблема

---

## 🎯 Выводы

**Главные уроки:**
1. **Контент священен** — никогда не менять без согласования
2. **Всегда использовать дизайн-систему** — не создавать новые компоненты
3. **Проверять перед сохранением** — сравнивать с оригиналом
4. **Спрашивать, если непонятно** — лучше уточнить, чем сделать неправильно

**Что работает хорошо:**
- ✅ Структура проекта (компоненты, документация)
- ✅ Чёткие правила в `AI_INSTRUCTIONS.md`
- ✅ Процесс восстановления контента из оригинала

**Что улучшить:**
- ⚠️ Добавить правила для изображений
- ⚠️ Добавить чек-лист SEO-элементов
- ⚠️ Улучшить чек-лист проверки перед сохранением

