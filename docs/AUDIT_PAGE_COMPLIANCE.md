# Технический аудит страницы — соответствие правилам проекта (инструкция для ИИ)

Цель: быстро и одинаково проверять, что конкретная HTML‑страница проекта соответствует канонам проекта (см. [DESIGN_SYSTEM.md](DESIGN_SYSTEM.md) и [AI_INSTRUCTIONS.md](../AI_INSTRUCTIONS.md)).

Эта инструкция описывает **как проводить аудит**. Она не заменяет сами правила.

---

## 0) Входные данные

Перед началом укажи (в отчёте аудита):
- Путь к странице (например: `src/html/.../page.html`)
- Группа страницы (главная / стиль / объект / блог / info)
- Эталон группы (см. PROJECT)

---

## 1) Как запускать аудит (workflow)

1. Открой страницу через Live Server из `src/html/...`.
2. Открой DevTools:
   - **Elements** (структура, атрибуты)
   - **Console** (ошибки JS)
   - **Network** (особенно изображения первого экрана)
   - **Lighthouse** (Performance + Accessibility) — при необходимости
3. Сверь страницу с документацией:
   - Каноны и компоненты: [DESIGN_SYSTEM.md](DESIGN_SYSTEM.md)
   - “Запрещено/разрешено” и краткие правила: [AI_INSTRUCTIONS.md](../AI_INSTRUCTIONS.md)
4. Зафиксируй результат:
  - Создай **отдельный файл‑отчёт аудита** в `docs/audits/`.
  - В `PROJECT.md` записывай только системные проблемы (влияет на 10+ страниц) как отдельную задачу.

**Важно:** во время аудита **не вносить правки в документацию**. Если документация неоднозначна/устарела — записать это как рекомендацию в отчёте аудита.

---

## 2) Что именно проверять (чек‑лист)

### A. Структура страницы

- `lang="ru"`, корректные `meta charset`/`viewport`.
- Есть `title`, `meta name="description"`.
- Есть `link rel="canonical"` (если это production‑страница).
- Временно закрыта от индексации (пока проект не на production): `meta robots noindex, nofollow`.
- Нет вложенных `.container` внутри `.container`.

### B. CSS и критический путь

- Основной CSS подключён как `css/output.css` из `src/html/css/output.css`.
- Критический CSS:
  - содержит только минимум (`:root`, `body`, `.sr-only`)
  - содержит только реально нужные стили для компонентов, которые есть на странице
  - **не разрастается** без причины.

### C. Иконки / SVG

- SVG только inline (`<svg>...</svg>`), нет `<img src="...svg">`.

Важно (чтобы не было двусмысленности): **не предлагать “вынести SVG в спрайт/отдельные файлы” как оптимизацию по умолчанию**. Текущий канон проекта — **inline SVG**. Если страница тяжёлая — оптимизируем сами inline SVG (SVGO/скрипты проекта), а не меняем подход.

### D. JS и интерактив

- Порядок скриптов:
  - если есть `el-*` → сначала `tailwindplus-elements.js` (type="module")
  - затем общий `nav.js`
- Нет page-specific inline‑JS для типовых блоков, которые уже делает `nav.js` (video cover, carousel и т.п.).

Примечание про legacy: на части страниц уже встречаются inline `onclick` (например, вызов `window.BxPopup(...)` или `dialog.showModal()`).
- Это **допустимо как наследие**.
- В аудите это **отмечаем** как замечание (если хотим привести к канону), но не считаем “поломкой”, если страница работает.
- Новые интерактивы по возможности добавляем через `nav.js`/хуки в разметке, а не через новый inline‑JS.

Отдельно про fallback‑код для `el-*` компонентов:
- Если на странице найден page-specific JS‑fallback для `el-*` (например `ensureTabsFallback` для `el-tab-group`), то в аудите **рекомендуем удалить этот код**.
- Причина: проект ориентируется на современные браузеры с поддержкой `type="module"`; функциональность `el-*` обеспечивается `tailwindplus-elements.js`.
- `nav.js` не ломается в Console (нет ошибок).

### E. Ссылки и базовая типографика

- Текстовые ссылки различимы: базовое правило — underline.
- Ссылки в тексте: без «кавычек» в тексте ссылки; стрелки (если используются) единообразны и по смыслу (`↑` внутри страницы, `↗` на другую страницу/внешний URL).
- Серый текст на белом фоне: избегать слишком светлого (`gray-400`) для обычного текста.

### F. Изображения (производительность и “отрисовка”)

- У всех `<img>` есть `width`/`height` (или контейнерный `aspect-*`) → нет CLS.
- По умолчанию: `decoding="async"`.
- По умолчанию: `loading="lazy"`.
- Для LCP/первого экрана:
  - **не** `loading="lazy"`
  - при необходимости `fetchpriority="high"`.
- Preload изображения первого экрана:
  - добавлять **только после проверки необходимости** (DevTools/Lighthouse)
  - если `fetchpriority="high"` достаточно — preload не нужен.
- `background-image` использовать только для декоративных фонов; контентные/SEO-важные и LCP — через `<img>/<picture>`.
- `srcset/sizes` — пока **на рассмотрении**, не требовать.

---

## Частые ловушки (помогает аудиту быть точнее)

- Путь к CSS: в рамках проекта канон — подключать CSS из `src/html/css/output.css`. Ссылки на `src/css/output.css` (вне `src/html`) считаем отклонением от канона Live Server/предпросмотра.
- `<dialog>`: `nav.js` добавляет доступность/обвязку, но **не является полифиллом поддержки** `HTMLDialogElement`. Совместимость нужно проверять отдельно.

---

## 3) Как оформлять результат (формат отчёта)

### Где хранить отчёты

- Все отчёты по страницам складываем в `docs/audits/`.
- Шаблон: `docs/audits/_TEMPLATE_PAGE_AUDIT.md`.
- Рекомендуемое имя файла:
  - `YYYY-MM-DD__<group>__<page>.md`
  - где `<page>` — имя HTML без `.html` (транслит уже есть в проекте).

### Что писать в отчёте

Рекомендуемый минимальный формат:

- Страница: `src/html/.../page.html`
- Статус: ✅ ок / ⚠️ есть замечания / 🔴 критично
- Замечания:
  - [ ] (кратко) что не так
  - [ ] (кратко) где именно
- Решение:
  - (кратко) что сделать

Дополнительно (если нужно): отдельным блоком "Рекомендации к документации" — **без внесения правок** в `*.md`.

Если замечание системное (влияет на 10+ страниц) — записать в `PROJECT.md` как отдельную задачу.

---

## 4) Правила точности

- Не придумывать контент/alt/title/URL: брать 1:1 с muse.ooo или оставлять согласованный плейсхолдер.
- Не менять `output.css` (генерат). Менять только `src/input.css`.
- Не править `tailwindplus-elements.js` (вендор).
