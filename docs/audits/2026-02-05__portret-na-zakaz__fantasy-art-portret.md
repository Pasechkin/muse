---
status: "✅"
page: "Портрет в стиле Fantasy Art"
path: "src/html/portret-na-zakaz/style/fantasy-art-portret.html"
date: "2026-02-05"
auditor: "Copilot (GPT-5 mini)"
summary: "Аудит страницы по чек-листу Task-1. Страница подготовлена, но закрыта от индексации и содержит заглушки внешних виджетов (Bitrix)."
---

Следующий отчёт подготовлен строго по чек-листу Task-1 (A..E).

**A. HEAD — мета, SEO, canonical**
- `lang="ru"` — OK.
- `charset` и `viewport` — OK.
- `title` и `meta description` — присутствуют и релевантны.
- **Примечание:** `<meta name="robots" content="noindex, nofollow">` (dev-режим) — преднамеренно и не считается проблемой.
- `link rel="canonical"` — присутствует и указывает на корректный URL.
- JSON-LD `Product` и `BreadcrumbList` — присутствуют (плюс для SEO).

**B. Критический CSS / стили**
- В head есть минимальный критический CSS (`:root` + `body`) — соответствует требованиям Task-1.
- Подключение единого CSS: `<link rel="stylesheet" href="../../css/output.css">` — OK.

**C. Скрипты**
- Внизу подключён `script defer src="../../js/nav.js"` — корректно (defer, внизу).
- Нет inline-блокирующих скриптов; JSON-LD в head допустим.
- Страница содержит заглушки для Bitrix (галерея, калькулятор, отзывы) — внешние виджеты должны загружаться отложенно и инициализироваться безопасно.

**D. Изображения и LCP**
- Предзагружено изображение героя через `<link rel="preload" as="image" href="https://muse.ooo/upload/img/o-fantasy-458-woman.webp" fetchpriority="high">` — правильно для LCP.- [ ] Первое (Hero) изображение НЕ имеет srcset/нарезки для маленьких экранов — 🔴 Нуждается в дополнительной проверке.- Hero `<img>` имеет `width`/`height`, `loading="eager"`, `decoding="async"`, `fetchpriority="high"` — согласовано.
- Галереи и миниатюры содержат `width`/`height` и `loading="lazy"` — соответствует чеклисту.

Рекомендация: после публикации проверить фактический LCP в Lighthouse и при необходимости скорректировать preload/fetchpriority.

**E. Семантика и доступность (a11y)**
- `aria-label` у кнопки «Наверх» — присутствует.
- Используются `sr-only` для навигации и кнопок — положительно.
- 🔴 Видео: требует дополнительной проверки
- При реализации модальных окон и виджетов использовать нативный `<dialog>` или доступные паттерны с управлением фокуса и aria-атрибутами.

---

**Actionables (по чек‑листу Task-1)**
1. Перед публикацией заменить `noindex, nofollow` → `index, follow`.
2. Внедрить реальные виджеты Bitrix вместо заглушек; загружать сторонний код асинхронно и инициализировать по событию/взаимодействию.
3. Проверить модальные диалоги на соответствие a11y (фокус, `aria-modal`, `role="dialog"`).
4. Прогнать Lighthouse (Mobile/Desktop) и зафиксировать метрики LCP/FCP и a11y; при необходимости добавить предзагрузку фактического LCP.

### Результат
- Статус: ✅
- Найденные проблемы: отсутствуют
- Замечания: 🔴 У первого изображения нет srcset/нарезки для маленьких экранов; 🔴 Видео: требует дополнительной проверки.
- Примечание: meta robots (dev-режим) и заглушки Bitrix — преднамеренные для разработки и не считаются проблемой.

Отчёт подготовлен в точном соответствии с `docs/AUDIT/Task-1.md`.
