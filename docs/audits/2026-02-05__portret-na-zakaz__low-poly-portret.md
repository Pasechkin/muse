---
status: "✅"
page: "Портрет в стиле лоу-поли"
path: "src/html/portret-na-zakaz/style/low-poly-portret.html"
date: "2026-02-05"
auditor: "Copilot (GPT-5 mini)"
summary: "Аудит страницы по чек-листу Task-1. Страница структурно корректна, но закрыта от индексации и содержит заглушки внешних виджетов." 
---

Документ подготовлен строго по чек‑листу Task-1 (A..E).

**A. HEAD — мета, SEO, canonical**
- `lang="ru"` — OK.
- `meta charset` и `viewport` — OK.
- `title` и `meta description` — присутствуют и релевантны.
- Meta robots="noindex, nofollow" (dev-режим) — преднамеренно и не считается проблемой.
- `link rel="canonical"` — присутствует и корректен.
- JSON-LD `Product` и `BreadcrumbList` — присутствуют (плюс для SEO).

**B. Критический CSS / стили**
- В head есть минимальный критический CSS (`:root` + `body`) — соответствует требованиям Task-1.
- Основной CSS подключён: `<link rel="stylesheet" href="../../css/output.css">` — OK.

**C. Скрипты и внешние ресурсы**
- Скрипты подключаются внизу с `defer` — корректно.
- На странице есть заглушки для Bitrix (галерея, калькулятор, отзывы) — при внедрении виджетов загружать сторонний код асинхронно и инициализировать по событию/взаимодействию.

Рекомендация: избегать блокирующей загрузки сторонних виджетов и инициализировать их динамически.

**D. Изображения и LCP**
- Hero preloaded: `<link rel="preload" as="image" href="https://muse.ooo/upload/img/low-poly-durov-458.webp" fetchpriority="high">` — корректно для ускорения LCP.
- [ ] Первое (Hero) изображение НЕ имеет srcset/нарезки для маленьких экранов — 🔴 Нуждается в дополнительной проверке.
- Hero `<img>` имеет `width`/`height`, `loading="eager"`, `decoding="async"`, `fetchpriority="high"` — соответствует чек‑листу.
- Второстепенные изображения используют `loading="lazy"` и указанные размеры — OK.

Рекомендация: после публикации прогнать Lighthouse и при необходимости скорректировать preloads/priority.

**E. Семантика и доступность (a11y)**
- `aria-label` у кнопки «Наверх» — присутствует.
- `sr-only` используется для вспомогательных текстов и навигации — положительный момент.
- 🔴 Видео: требует дополнительной проверки
- При добавлении модальных окон/виджетов обеспечить управление фокусом и `aria-modal/role="dialog"`.

---

**Actionables (по чек‑листу Task-1)**
1. Перед публикацией заменить `meta[name=robots]` на `index, follow`.
2. Заменить заглушки Bitrix реальной интеграцией и загружать сторонние виджеты асинхронно.
3. Прогнать страницу через Lighthouse (Mobile + Desktop) и зафиксировать LCP/FCP и a11y; при необходимости добавить оптимизации.

### Результат
- Статус: ✅
- Найденные проблемы: отсутствуют
- Замечания: 🔴 У первого изображения нет srcset/нарезки для маленьких экранов; 🔴 Видео: требует дополнительной проверки.
- Примечание: meta robots (dev-режим) и заглушки Bitrix — преднамеренные для разработки и не считаются проблемой.

Отчёт составлен в строгом соответствии с `docs/AUDIT/Task-1.md`.


## Дополнение 2026-02-06
Статус: ✅
Проблемы: устранены (content-auto/hero decoding/el-*).
