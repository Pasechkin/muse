---
status: "✅"
page: "Портрет акварелью"
path: "src/html/portret-na-zakaz/style/portret-akvarelyu.html"
date: "2026-02-05"
auditor: "Copilot (GPT-5 mini)"
summary: "Аудит страницы по чек‑листу Task-1. Страница структурно готова, но временно закрыта от индексации и содержит заглушки сторонних виджетов." 
---

Документ подготовлен строго по чек‑листу Task-1 (A..E).

**A. HEAD — мета, SEO, canonical**
- `lang="ru"` — OK.
- `charset` и `viewport` — OK.
- `title` и `meta description` — присутствуют и релевантны.
- Meta robots="noindex, nofollow" (dev-режим) — преднамеренно и не считается проблемой.
- `link rel="canonical"` — присутствует и корректен.
- JSON-LD `Product` и `BreadcrumbList` — присутствуют и корректно оформлены.

**B. Критический CSS / стили**
- В head есть минимальный критический CSS (`:root` + `body`) — соответствует требованиям Task-1.
- Основной CSS подключён: `<link rel="stylesheet" href="../../css/output.css">` — OK.

**C. Скрипты и внешние ресурсы**
- Скрипты подключаются внизу (навигация) с `defer` — соответствует требованиям.
- На странице присутствуют заглушки Bitrix для галерей и калькулятора — внешние виджеты должны загружаться асинхронно и инициализироваться безопасно.

Рекомендация: загружать виджеты по событию (`DOMContentLoaded` или по взаимодействию) и избегать блокирования рендера.

**D. Изображения и LCP**
- Hero предзагружается через `<link rel="preload" as="image" ... fetchpriority="high">` — правильно.
- [ ] Первое (Hero) изображение НЕ имеет srcset/нарезки для маленьких экранов — 🔴 Нуждается в дополнительной проверке.
- Hero `<img>` имеет `width`/`height`, `loading="eager"`, `decoding="async"`, `fetchpriority="high"` — соответствие LCP.
- Миниатюры и второстепенные изображения используют `loading="lazy"` и указанные размеры — OK.

Рекомендация: после публикации проверить LCP в Lighthouse и скорректировать preloads при необходимости.

**E. Семантика / доступность (a11y)**
- `aria-label` у кнопки «Наверх» — присутствует.
- `sr-only` используется для навигации и вспомогательных элементов — положительный момент.
- При добавлении модальных окон и виджетов обеспечить управление фокусом и `aria-modal/role="dialog"`.

---

**Actionables (по чек‑листу Task-1)**
1. Перед публикацией изменить `meta[name=robots]` на `index, follow`.
2. Реализовать реальные интеграции Bitrix вместо заглушек; сторонний код загружать отложенно.
3. Прогнать страницу через Lighthouse (Mobile + Desktop) и зафиксировать LCP/FCP и a11y; при необходимости добавить дополнительные preloads или оптимизации CSS.

### Результат
- Статус: ✅
- Найденные проблемы: отсутствуют
- Замечания: 🔴 У первого изображения нет srcset/нарезки для маленьких экранов.
- Примечание: meta robots (dev-режим) и заглушки Bitrix — преднамеренные для разработки и не считаются проблемой.

Отчёт подготовлен строго по `docs/AUDIT/Task-1.md`.


## Дополнение 2026-02-06
Статус: ✅
Проблемы: устранены (content-auto/hero decoding/el-*).
