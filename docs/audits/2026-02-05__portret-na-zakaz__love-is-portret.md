---
status: "✅"
page: "Love is... портрет"
path: "src/html/portret-na-zakaz/style/love-is-portret.html"
date: "2026-02-05"
auditor: "Copilot (GPT-5 mini)"
summary: "Аудит страницы по чек-листу Task-1. Страница корректна, но закрыта от индексации и содержит заглушки внешних виджетов." 
---

Отчёт подготовлен строго по чек‑листу Task-1 (A..E).

### Результат
- Статус: ✅
- Найденные проблемы: отсутствуют
- Замечания: 🔴 У первого изображения нет srcset/нарезки для маленьких экранов.
- Примечание: meta robots (dev-режим) и заглушки Bitrix — преднамеренные для разработки и не считаются проблемой.
**A. HEAD — мета, SEO, canonical**
- `lang="ru"` — OK.
- `charset` и `viewport` — OK.
- `title` и `meta description` — присутствуют и релевантны.
- Meta robots="noindex, nofollow" (dev-режим) — преднамеренно и не считается проблемой.
- `link rel="canonical"` — присутствует и корректен.
- JSON-LD `Product` и `BreadcrumbList` — присутствуют (плюс для SEO).

**B. Критический CSS / стили**
- Критический CSS в head ограничен `:root` и `body` — соответствует требованиям Task-1.
- Основной CSS подключён: `<link rel="stylesheet" href="../../css/output.css">` — OK.

**C. Скрипты и внешние ресурсы**
- Внизу подключается навигационный скрипт с `defer` — корректно.
- На странице есть заглушки Bitrix (галерея, калькулятор, отзывы) — внедрять внешние виджеты нужно через отложенную загрузку и безопасную инициализацию.

Рекомендация: загружать сторонний код по событию или взаимодействию, не блокируя рендер.

**D. Изображения и LCP**
- Пре‑загрузка героя: `<link rel="preload" as="image" href="https://muse.ooo/upload/img/o-love-is-458.webp" fetchpriority="high">` — правильно.
- [ ] Первое (Hero) изображение НЕ имеет srcset/нарезки для маленьких экранов — 🔴 Нуждается в дополнительной проверке.
- Hero `<img>` имеет `width`/`height`, `loading="eager"`, `decoding="async"`, `fetchpriority="high"` — соответствует LCP-практикам.
- Миниатюры и второстепенные изображения используют `loading="lazy"` и указанные размеры — OK.

Рекомендация: после публикации прогнать Lighthouse и проверить, что предзагруженное изображение совпадает с LCP; при расхождении скорректировать preloads.

**E. Семантика и доступность (a11y)**
- `aria-label` у кнопки «Наверх» — присутствует.
- `sr-only` используется для навигационных ссылок и элементов управления — положительный момент.
- При реализации модалов и сторонних виджетов обеспечить управление фокусом и `aria-modal`/`role="dialog"`.

---

**Actionables (по чек‑листу Task-1)**
1. Перед публикацией заменить `meta[name=robots]` на `index, follow`.
2. Реализовать рабочую интеграцию Bitrix вместо заглушек и загружать виджеты асинхронно.
3. Прогнать страницу через Lighthouse (Mobile + Desktop) и зафиксировать LCP/FCP и a11y; при необходимости добавить оптимизации.

Отчёт составлен строго по `docs/AUDIT/Task-1.md`.
