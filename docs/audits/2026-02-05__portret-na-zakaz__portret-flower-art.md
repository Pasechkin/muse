---
status: "✅"
page: "Портрет Flower Art"
path: "src/html/portret-na-zakaz/style/portret-flower-art.html"
date: "2026-02-05"
auditor: "Copilot"
summary: "Аудит по чек‑листу Task-1: страница имеет корректную структуру, подготовлена к LCP-оптимизациям, но закрыта от индексации и содержит заглушки внешних виджетов (Bitrix)."
---

Отчёт подготовлен в формате Task-1 (A..E).

**A. HEAD — мета, SEO, canonical**

### Результат
- Статус: ✅
- Найденные проблемы: отсутствуют
- Замечания: 🔴 У первого изображения нет srcset/нарезки для маленьких экранов.
- Примечание: meta robots (dev-режим) и заглушки Bitrix — преднамеренные для разработки и не считаются проблемой.
- `lang="ru"` — OK.
- `meta charset` и `viewport` — OK.
- `title` и `meta description` — присутствуют и релевантны.
- Meta robots="noindex, nofollow" (dev-режим) — преднамеренно и не считается проблемой.
- `link rel="canonical"` — присутствует и корректен.
- JSON‑LD `Product` и `BreadcrumbList` — присутствуют и выглядят корректно.

**B. Критический CSS / стили**
- В head есть минимальный критический CSS (переменные `:root`, базовые `body`) — соответствует требованиям.
- Основной CSS подключён как `../../css/output.css` (Tailwind v4) — OK.

**C. Скрипты и внешние ресурсы**
- `nav.js` подключён с `defer` — правильно.
- На странице несколько заглушек Bitrix: галерея, калькулятор, отзывы. Пока это блоки-заплатки — важно загружать реальные виджеты асинхронно по необходимости.

Рекомендация: внедрять интеграцию Bitrix с ленивой загрузкой (динамическая подгрузка скриптов по событию/вкладке), чтобы не ухудшать LCP.

**D. Изображения и LCP**
- Предзагрузка хиро-изображения: `<link rel="preload" as="image" href="https://muse.ooo/upload/img/o-flower-art-4-458.webp" fetchpriority="high">` — корректно для LCP.
- [ ] Первое (Hero) изображение НЕ имеет srcset/нарезки для маленьких экранов — 🔴 Нуждается в дополнительной проверке.
- Hero `<img>` имеет `width="458" height="550"`, `loading="eager"`, `decoding="async"`, `fetchpriority="high"` — оптимизация LCP выполнена.
- Дополнительные изображения указывают размеры и используют `loading="lazy"` — хорошо.

Рекомендация: после выката проверить LCP в Lighthouse и скорректировать preload/fetchpriority при необходимости.

**E. Семантика / доступность (a11y)**
- Кнопка «Наверх» имеет `aria-label="Наверх"` — OK.
- Навигация по странице использует `sr-only` для заголовков в ссылках — положительный момент.
- Before/After слайдер содержит `input type="range" aria-label="Сравнение до и после"` — базовая доступность обеспечена.
- При реализации реальных модальных окон/виджетов обеспечить управление фокусом и `aria-modal`/`role="dialog"`.

---

**Actionables**
1. Перед публикацией заменить `meta[name=robots]` на `index, follow`.
2. Заменить заглушки Bitrix на рабочую интеграцию; загружать сторонние виджеты лениво/по требованию.
3. Прогнать страницу через Lighthouse (Mobile + Desktop) и зафиксировать LCP/FCP, a11y, best-practices; при необходимости скорректировать предзагрузки.
4. Проверить модальные/всплывающие виджеты на корректное управление фокусом и keyboard-access.

Отчёт составлен в соответствии с `docs/AUDIT/Task-1.md`.
