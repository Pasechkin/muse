---
status: "✅"
page: "Портрет карандашом"
path: "src/html/portret-na-zakaz/style/portret-karandashom.html"
date: "2026-02-05"
auditor: "Copilot"
summary: "Аудит Task-1: страница структурирована корректно, LCP оптимизации присутствуют; пока закрыта от индексации и содержит заглушки Bitrix." 
---

Отчёт составлен в строгом соответствии с `docs/AUDIT/Task-1.md` (разделы A..E).

**A. HEAD — мета, SEO, canonical**
- `lang="ru"` — OK.
- `meta charset` и `viewport` — OK.
- `title` и `meta description` — присутствуют и релевантны.
- Meta robots="noindex, nofollow" (dev-режим) — преднамеренно и не считается проблемой.
- `link rel="canonical"` — присутствует и корректен.
- JSON‑LD `Product` и `BreadcrumbList` — присутствуют и корректно оформлены.

**B. Критический CSS / стили**
- В head есть минимальный критический CSS (`:root`, `body`) — соответствует требованиям.
- Основной CSS подключён `../../css/output.css` — OK.

**C. Скрипты и внешние ресурсы**
- `nav.js` подключён с `defer` — правильно.
- Страница содержит заглушки Bitrix для галереи и калькулятора — рекомендуется внедрять ленивую загрузку виджетов.

Рекомендация: загружать внешние виджеты по взаимодействию/видимости и использовать защитную инициализацию (проверка элементов, try/catch).

**D. Изображения и LCP**
- Предзагрузка хиро-изображения: `<link rel="preload" as="image" href="https://muse.ooo/upload/img/o-pencil-458.webp" fetchpriority="high">` — корректно.
- [ ] Первое (Hero) изображение НЕ имеет srcset/нарезки для маленьких экранов — 🔴 Нуждается в дополнительной проверке.
- Hero `<img>` имеет `width`/`height`, `loading="eager"`, `decoding="async"`, `fetchpriority="high"` — оптимизация LCP предусмотрена.
- Before/After изображения и миниатюры имеют размеры и `loading="lazy"` — OK.

Рекомендация: прогнать страницу через Lighthouse после публикации и проверить реальные метрики LCP/FCP.

**E. Семантика / доступность (a11y)**
- Кнопка «Наверх» имеет `aria-label="Наверх"` — OK.
- Breadcrumb использует `nav` с `aria-label="Breadcrumb"` — OK.
- Before/After slider использует `input type="range" aria-label="Сравнение до и после"` — базовая доступность есть; проверить состояние фокуса и клавиатурную навигацию.

---

**Actionables**
1. Перед публикацией заменить `meta[name=robots]` на `index, follow`.
2. Реализовать интеграцию Bitrix и загружать сторонние виджеты лениво по видимости/взаимодействию.
3. Прогнать Lighthouse (Mobile + Desktop) и зафиксировать LCP/FCP и a11y; при необходимости скорректировать предзагрузки.
4. Проверить, что JS инициализации (сайд-эффекты) защищены проверками наличия DOM-элементов.

### Результат
- Статус: ✅
- Найденные проблемы: отсутствуют
- Замечания: 🔴 У первого изображения нет srcset/нарезки для маленьких экранов.
- Примечание: meta robots (dev-режим) и заглушки Bitrix — преднамеренные для разработки и не считаются проблемой.

Отчёт подготовлен по `docs/AUDIT/Task-1.md`.


## Дополнение 2026-02-06
Статус: ✅
Проблемы: устранены (content-auto/hero decoding/el-*).
