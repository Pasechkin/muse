---
status: "✅"
page: "Портрет из слов"
path: "src/html/portret-na-zakaz/style/portret-iz-slov.html"
date: "2026-02-05"
auditor: "Copilot"
summary: "Аудит Task-1: структура и оптимизации LCP выполнены, но страница закрыта от индексации и использует заглушки Bitrix; есть внешние ассеты (Drift) загружаемые асинхронно." 
---

Отчёт подготовлен в соответствии с `docs/AUDIT/Task-1.md` (A..E).

**A. HEAD — мета, SEO, canonical**
- `lang="ru"` — OK.
- `meta charset` и `viewport` — OK.
- `title` и `meta description` — присутствуют и релевантны.
- Meta robots="noindex, nofollow" (dev-режим) — преднамеренно и не считается проблемой.
- `link rel="canonical"` — присутствует и корректен.
- JSON‑LD `Product` и `BreadcrumbList` — присутствуют и корректно оформлены.

**B. Критический CSS / стили**
- В head присутствует минимальный критический CSS (`:root`, `body`) — соответствует требованиям.
- Основной CSS подключён `../../css/output.css` (Tailwind v4) — OK.
- Внешний CSS `drift-basic.min.css` подключён с `media="print" onload="this.media='all'"` — корректный приём для ненадежной/ленивой загрузки, не блокирует LCP.

**C. Скрипты и внешние ресурсы**
- `nav.js` подключён с `defer` — OK.
- `Drift.min.js` загружается с `defer` и инициализируется в `onload` — не блокирует парсинг, но проверяйте, что ошибки инициализации не ломают страницу при отсутствии элементов.
- Страница содержит заглушки Bitrix (галерея, калькулятор, отзывы) — нужно реализовать ленивую интеграцию виджетов.

Рекомендация: загружать сторонние виджеты по взаимодействию/видимости, проверять try/catch вокруг инициализации `Drift`.

**D. Изображения и LCP**
- Предзагрузка хиро-изображения: `<link rel="preload" as="image" href="https://muse.ooo/upload/img/portret-iz-slov-458-2.webp" fetchpriority="high">` — правильно.
- [ ] Первое (Hero) изображение НЕ имеет srcset/нарезки для маленьких экранов — 🔴 Нуждается в дополнительной проверке.
- Hero `<img>` имеет `width`/`height`, `loading="eager"`, `decoding="async"`, `fetchpriority="high"` — настроено для ускорения LCP.
- Миниатюры и before/after изображения используют `loading="lazy"` и указанные размеры — OK.

Рекомендация: провести Lighthouse после выката и скорректировать preload/fetchpriority при необходимости.

**E. Семантика / доступность (a11y)**
- Кнопка «Наверх» содержит `aria-label="Наверх"` — OK.
- Breadcrumb использует `nav` с `aria-label="Breadcrumb"` — OK.
- Before/After slider использует `input type="range" aria-label="Сравнение до и после"` — базовая доступность есть; проверьте видимость фокуса и клавиатурную управляемость.
- При использовании `Drift` (увеличение изображений) убедитесь, что фокус и события клавиатуры корректно обрабатываются для a11y.

---

**Actionables**
1. Перед публикацией заменить `meta[name=robots]` на `index, follow`.
2. Реализовать рабочую интеграцию Bitrix и загружать внешние виджеты лениво (по клику/видимости).
3. Добавить защитные проверки (try/catch и проверку наличия элементов) при инициализации `Drift` в `initDrift()`.
4. Прогнать страницу через Lighthouse (Mobile + Desktop) и зафиксировать LCP/FCP, a11y; при необходимости скорректировать загрузку ресурсов.

### Результат
- Статус: ✅
- Найденные проблемы: отсутствуют
- Замечания: 🔴 У первого изображения нет srcset/нарезки для маленьких экранов.
- Примечание: meta robots (dev-режим) и заглушки Bitrix — преднамеренные для разработки и не считаются проблемой.

Отчёт составлен в строгом соответствии с `docs/AUDIT/Task-1.md`.


## Дополнение 2026-02-06
Статус: ⚠️
Проблемы:
- C: есть inline JS (`onload`) для Drift CSS/JS. Drift на странице нужен, но inline-обработчики нарушают чек-лист.
