# 🚀 Инструкция по загрузке на GitHub

## Шаг 1: Установка Git (если еще не установлен)

1. Скачайте Git: https://git-scm.com/download/win
2. Установите с настройками по умолчанию
3. Перезапустите PowerShell/Terminal

## Шаг 2: Создание репозитория на GitHub

1. Зайдите на https://github.com
2. Войдите в аккаунт
3. Нажмите "+" → "New repository"
4. Назовите, например: `muse-site`
5. Выберите "Public"
6. **НЕ** ставьте галочки на "Add README", "Add .gitignore" (у нас уже есть)
7. Нажмите "Create repository"

## Шаг 3: Загрузка проекта

### Вариант А: Через командную строку (PowerShell)

Откройте PowerShell в папке `tailwind-project` и выполните:

```powershell
# Инициализация git
git init

# Добавление всех файлов
git add .

# Первый коммит
git commit -m "Initial commit: Muse site with Tailwind CSS"

# Переименование ветки в main (если нужно)
git branch -M main

# Подключение к вашему GitHub репозиторию
# ЗАМЕНИТЕ URL на ваш реальный репозиторий!
git remote add origin https://github.com/ВАШ_ЛОГИН/muse-site.git

# Загрузка на GitHub
git push -u origin main
```

**Важно:** Замените `ВАШ_ЛОГИН` и `muse-site` на ваши реальные данные!

### Вариант Б: Через GitHub Desktop

1. Скачайте: https://desktop.github.com/
2. Установите и войдите в аккаунт
3. File → Add Local Repository
4. Выберите папку `tailwind-project`
5. Внизу напишите: "Initial commit"
6. Нажмите "Commit to main"
7. Нажмите "Publish repository"
8. Выберите ваш репозиторий на GitHub
9. Готово!

## Шаг 4: Настройка GitHub Pages (опционально)

После загрузки на GitHub:

1. Откройте ваш репозиторий на GitHub
2. Settings → Pages
3. Source: Deploy from a branch
4. Branch: `main` или `gh-pages` (после первого деплоя)
5. Folder: `/ (root)` или `/src/html` (если используете Actions)
6. Save

Автоматический деплой через GitHub Actions уже настроен в `.github/workflows/deploy.yml`!

## ✅ Что уже готово:

- ✅ `.gitignore` - настроен, node_modules не попадет в репозиторий
- ✅ GitHub Actions workflow - автоматический деплой настроен
- ✅ Vercel config - готов к деплою на Vercel
- ✅ Netlify config - готов к деплою на Netlify

## 🎯 Следующий шаг: Деплой на Vercel (рекомендуется)

После загрузки на GitHub:

1. Зайдите на https://vercel.com
2. Войдите через GitHub
3. Import Project
4. Выберите ваш репозиторий `muse-site`
5. Настройки (уже указаны в vercel.json):
   - Build Command: `npm run build:once && npm run copy-css`
   - Output Directory: `src/html`
6. Deploy

Через 2 минуты сайт будет доступен по адресу: `muse-site.vercel.app`




