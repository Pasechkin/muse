# Инструкции для ИИ-агента

## ⚠️ ВАЖНО: Прочитай перед началом работы

### 1. Источник истины и приоритеты

**Единственный источник по дизайну, CSS, компонентам, классам, примерам и чек‑листам —** `docs/DESIGN_SYSTEM.md`.

**В AI_INSTRUCTIONS не дублируем детали дизайн‑системы.**
Если правила меняются — обновляем только `docs/DESIGN_SYSTEM.md`.

При необходимости статуса/реестров:
- `PROJECT.md` — статус, исключения, эталоны.
- `PROJECT.md` (раздел "Список страниц для переверстки") — список страниц.


### 2. НЕ изменяй документацию

❌ **ЗАПРЕЩЕНО** изменять файлы документации (`*.md`) без явного согласования с пользователем.

Если считаешь, что нужно что-то добавить в документацию — **сначала спроси**.

### 2.1 Безопасность публикации на GitHub

❌ **ЗАПРЕЩЕНО** выполнять действия по публикации на GitHub, которые приводят к массовой перезаписи файлов на локальном диске ПК.

### 3. Эталоны по группам страниц

Эталоны и их список — только в [PROJECT.md](PROJECT.md#эталоны-по-группам-страниц).

### 4. Что НЕ анализировать при аудите

| Элемент | Причина |
|---------|---------|
| **Header / Footer** | Будут добавлены только на главной после интеграции с 1С-Битрикс |
| **OAuth модальное окно** | Только на эталоне `portret-maslom.html`, на остальных страницах удалено |
| **Калькулятор (секция "Цена")** | Калькулятор реализован и работает. Аудит вёрстки калькулятора — отдельно |

---

## Процессные правила (без дублирования дизайн‑системы)

### Контент, верстка, CSS, компоненты

Все конкретные правила, классы, примеры и чек‑листы берём только из `docs/DESIGN_SYSTEM.md`.
В AI_INSTRUCTIONS детали не повторяем.

### Предупреждай об изменениях

Если что-то нужно изменить или убрать — **СНАЧАЛА СПРОСИ**.

Если количество элементов отличается от оригинала — **ПРЕДУПРЕДИ**.

### ⚠️ НЕ сокращай код без предупреждения

❌ **ЗАПРЕЩЕНО** сокращать, упрощать или "оптимизировать" JavaScript, CSS или HTML без явного согласования.

Примеры запрещённых действий:
- Убирать "лишние" обработчики событий
- Упрощать логику скриптов
- Удалять "ненужные" проверки и защиты
- Сокращать CSS правила

**Если считаешь, что код можно упростить — СНАЧАЛА СПРОСИ и объясни что именно хочешь убрать.**

### 🔧 Калькулятор: порядок внесения изменений

**Source of truth разметки калькулятора — `src/html/calc.html`.**

**ОБЯЗАТЕЛЬНЫЙ порядок:**
1. Изменения HTML/CSS калькулятора вносить **СНАЧАЛА в `calc.html`**, проверить.
2. **ЗАТЕМ** тиражировать на продуктовые страницы (`foto-na-kholste-sankt-peterburg.html`, `portret-maslom.html` и др.).

**ЗАПРЕЩЕНО:**
- ❌ Менять разметку калькулятора **только** на продуктовой странице без обновления `calc.html`.
- ❌ Оставлять расхождения между `calc.html` и продуктовыми страницами.

**Файлы калькулятора:**
- Разметка (эталон): `src/html/calc.html`
- JS-движок: `src/html/js/calc.js`
- Конфиг цен: `src/html/js/prices.js`
- Загрузчик фото: `src/html/js/uploader.js`

**Исключение:** изменения, специфичные ТОЛЬКО для конкретной страницы (например, `CalcInit({ type: 'portrait', tooltips })` в inline-скрипте), — вносятся на продуктовой странице напрямую.
