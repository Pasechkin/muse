# 📤 Как загрузить БЕЗ node_modules (только нужные файлы)

## ⚠️ ВАЖНО: node_modules НЕ нужна для GitHub!

Папка `node_modules` содержит тысячи файлов и весит сотни мегабайт. Её НЕ нужно загружать!

## ✅ Что нужно загрузить:

Только эти файлы и папки:

```
tailwind-project/
├── src/
│   ├── html/          ← ВСЯ папка (но БЕЗ node_modules внутри)
│   │   ├── index.html
│   │   ├── css/
│   │   ├── portret-na-zakaz/
│   │   └── info/
│   └── input.css     ← Этот файл
├── tailwind.config.js
├── postcss.config.js
├── package.json
├── vercel.json
├── netlify.toml
├── .gitignore        ← Этот файл важен!
├── README.md
└── ИНСТРУКЦИЯ.md
```

## 🚫 Что НЕ загружать:

- ❌ `node_modules/` - НЕ загружать!
- ❌ `package-lock.json` - можно не загружать

## 📝 Способ 1: Через GitHub Desktop (рекомендуется)

GitHub Desktop автоматически игнорирует node_modules благодаря .gitignore:

1. Скачайте [GitHub Desktop](https://desktop.github.com/)
2. Установите и войдите
3. File → Add Local Repository
4. Выберите папку `tailwind-project`
5. GitHub Desktop покажет только файлы, которые будут загружены
6. Убедитесь, что `node_modules` НЕ в списке
7. Нажмите "Commit to main" → "Publish repository"

## 📝 Способ 2: Через веб-интерфейс (вручную)

Если загружаете через браузер:

1. Создайте репозиторий на GitHub
2. Откройте папку `tailwind-project`
3. **НЕ перетаскивайте всю папку!**
4. Загружайте файлы по отдельности или маленькими группами:
   - Сначала все файлы из корня (package.json, .gitignore и т.д.)
   - Потом папку `src` (но БЕЗ node_modules)
   - Внутри `src` загрузите `html` и `input.css`

## 📝 Способ 3: Создать копию БЕЗ node_modules

Создам скрипт, который скопирует только нужные файлы:

1. Откройте файл `СКОПИРОВАТЬ_БЕЗ_NODE_MODULES.bat` (создам ниже)
2. Дважды кликните на него
3. Появится папка `tailwind-project-clean` БЕЗ node_modules
4. Загрузите эту папку в GitHub


