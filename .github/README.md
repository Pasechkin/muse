# GitHub Configuration

Эта папка содержит конфигурационные файлы для GitHub репозитория.

## Файлы

### CODEOWNERS

Определяет владельцев кода для разных частей репозитория. При создании Pull Request владельцы автоматически запрашиваются для review.

**Текущая настройка:** Все файлы требуют одобрения @Pasechkin

### workflows/

#### deploy.yml
Автоматический деплой на GitHub Pages при пуше в ветки `main` или `master`.

#### pr-checks.yml
Автоматические проверки для Pull Requests:
- Установка зависимостей
- Сборка CSS (Tailwind)
- Проверка наличия output файлов
- Базовая валидация структуры проекта

**Назначение:** Гарантирует, что все PR проходят базовые проверки перед мерджем.

## Рекомендуемая настройка Branch Protection

Для максимальной защиты кода настройте Branch Protection Rules в GitHub:

1. Перейдите в **Settings** → **Branches**
2. Добавьте правило для ветки `main` (или `master`)
3. Включите:
   - ✅ Require a pull request before merging
   - ✅ Require approvals (минимум 1)
   - ✅ Require status checks to pass before merging
     - Отметьте: `Build and Validate`
   - ✅ Require conversation resolution before merging
   - ✅ Require review from Code Owners
   - ✅ Do not allow bypassing the above settings

Подробная инструкция: см. файл `ЗАЩИТА_РЕПОЗИТОРИЯ.md` в корне репозитория.

## Дополнительная информация

Для получения подробных инструкций по защите репозитория смотрите главный документ:
📄 **[ЗАЩИТА_РЕПОЗИТОРИЯ.md](../ЗАЩИТА_РЕПОЗИТОРИЯ.md)**
