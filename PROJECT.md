# Проект Muse — Переверстка на Tailwind CSS

> **Для ИИ-агента:** Перед созданием новой страницы прочитай [AI_INSTRUCTIONS.md](AI_INSTRUCTIONS.md)

## Основная задача

Переверстать сайт **www.muse.ooo** с Bootstrap 3 (тема Stack) на Tailwind CSS.

### Ключевые требования

1. Сохранить визуальный стиль оригинала (цвета, типографика, компоновка)
2. Пошаговый подход: сначала статические страницы, потом интеграция с 1C-Bitrix
3. Деплой: GitHub → Vercel
4. Интеграцию с Bitrix будет делать программист (не в рамках этого проекта)

---

## Текущий статус

**Дата обновления:** 23 января 2026

### Прогресс

| Категория | Готово | Всего |
|-----------|--------|-------|
| Главные страницы | 4 | 4 |
| Стили портретов | 18 | 18 |
| Объекты портретов | 5 | 5 |
| Блог | 20 | 20 |
| Info | 3 | 3 |
| Вспомогательные | 2 | 2 |
| **Итого** | **55** | **59** |

### Что готово

- ✅ **55 страниц** полностью переверстаны на Tailwind CSS
- ✅ **Дизайн-система v2** — переезд на Tailwind v4 и `@layer components`
- ✅ **Архитектурная навигация (arch-nav)** — замена точек на линии с текстом
- ✅ **Оптимизация (DRY)** — вынос повторяющихся стилей из HTML в `input.css`
- ✅ **Скрипты** — `js/nav.js` переведен на Intersection Observer

### В работе (Рефакторинг)

- 🔄 **Чистка HTML** — удаление лишнего кода и замена навигации во всех 55 файлах
- 🔄 **Компонентный рефакторинг (DRY)** — унификация повторяющихся элементов на 18 стилях портретов
- ⏳ **4 страницы** — региональные (Москва), доставка, оплата

#### 📋 Чек-лист компонентного рефакторинга

**Эталон:** `src/html/portret-na-zakaz/style/portret-maslom.html`

| Этап | Задача | Статус |
|------|--------|--------|
| **0. Контейнеры** | Исправить `.container` в `input.css` (добавить `max-width: 1170px`). Убрать 11 двойных оберток в HTML. | ⏳ Ждет |
| **1. Кнопки** | Создать `.btn-primary`, `.btn-outline` в `@layer components`. Заменить длинные классы в HTML. | ⏳ Ждет |
| **2. Как заказать** | Внедрить `.step-container`, `.process-step`. Чистим разметку шагов и линию между ними. | ⏳ Ждет |
| **3. Преимущества** | Создать `.feature-card`. Убрать дублирование 6 карточек (15 строк → 7 строк каждая). | ⏳ Ждет |
| **4. Характеристики** | Внедрить `.check-list`. Заменить SVG-галочки в HTML на CSS-символ. | ⏳ Ждет |
| **5. Навигация** | Проверить `arch-nav` и Intersection Observer на разных разрешениях экрана. | ⏳ Ждет |
| **6. Масштабирование** | Применить изменения к 17 другим стилям в `portret-na-zakaz/style/`. | ⏳ Ждет |

**Обнаруженные проблемы:**
- 🔴 79% секций имеют двойные контейнеры (`mx-auto px-4 max-w-[1170px]` + `.container`)
- 🔴 Кнопки дублируют ~120 символов классов вместо 1 компонента
- 🔴 Карточки преимуществ: 6×15 строк кода можно сократить до 6×7 строк

**Ожидаемый результат:**
- Размер HTML: ~1015 строк → ~600 строк (-40%)
- Поддержка: изменения в 1 файле вместо 18
- Консистентность: единый стиль на всех страницах

---

**Полный список:** см. [docs/PAGES_LIST.md](docs/PAGES_LIST.md)

### Цели рефакторинга

Основная цель рефакторинга — переход к **компонентному подходу** и обеспечение чистоты кода.

-   **Источник:** [Styling with utility classes (Tailwind Docs)](https://tailwindcss.com/docs/styling-with-utility-classes#overview)
-   **Референс:** Принципы модульности из `catalyst-ui-kit`.
-   **Стек:** Проект использует только **HTML, CSS и JavaScript**, без фреймворков типа React.

---

## Структура проекта

```
tailwind-project/
├── src/
│   ├── html/                  # Готовые HTML-страницы
│   │   ├── css/output.css     # Скомпилированный CSS
│   │   ├── js/nav.js          # Скрипты для навигации (`arch-nav`) и UI
│   │   └── ... (папки страниц)
│   └── input.css              # Исходный CSS для Tailwind
├── docs/                      # Документация проекта
└── package.json               # Зависимости
```

---

## Технические особенности

### CSS

- **Для разработки:** Tailwind CDN (`<script src="https://cdn.tailwindcss.com">`)
- **Для продакшена:** Скомпилированный `output.css`
- **Настройки:** Цвета, контейнер, типографика в `tailwind.config.js`
- **Сборка:** Проект использует **локальную сборку** CSS через Tailwind CLI.
- **Исходный файл:** `src/input.css` (содержит директивы `@tailwind` и кастомные стили).
- **Конфигурация:** Настройки (цвета, шрифты, контейнер) определяются напрямую в `src/input.css` с помощью директивы `@theme` и `@layer` (архитектура Tailwind v4).
- **Результат:** Скомпилированный файл `src/html/css/output.css`, который подключается ко всем HTML-страницам.
- **CDN:** **Не используется** (кроме Tailwind Plus Elements для мобильного меню).

### Скрипты

- **Общие функции:** `js/nav.js` (Page Navigator, Back to Top)
- **Inline скрипты:** Video Cover, Carousel Scroll, Video Modal, Typed.js
- **Tailwind Plus Elements:** CDN для мобильного меню
- **Основной файл:** `src/html/js/nav.js`. Содержит всю логику для интерактивных компонентов:
    - Архитектурная навигация (`arch-nav`)
    - Кнопка "Наверх" (Back to Top)
    - Video Cover
    - Carousel Scroll
    - Video Modal
    - Typed.js
- **Сторонние скрипты:** CDN для Tailwind Plus Elements (мобильное меню).
- **План рефакторинга:** Перенести все сторонние скрипты (включая Tailwind Elements) с CDN на локальную установку через `npm` для финальной сборки.

### Брейкпоинты (стандартные Tailwind)

| Класс | Ширина | Использование |
|-------|--------|---------------|
| `sm` | 640px | Мобильные (горизонтально) |
| `md` | 768px | Планшеты |
| `lg` | 1024px | Ноутбуки |
| `xl` | 1280px | Большие мониторы |

### Контейнер

- Максимальная ширина: **1170px**
- Padding: **15px** с каждой стороны
- Класс: `container`

---

## Деплой

### Быстрый способ

Запустить **`БЫСТРЫЙ_ДЕПЛОЙ.bat`** — он сделает всё автоматически.

### Команды

```bash
npm install          # Установка зависимостей
npm run build        # Сборка CSS (с отслеживанием)
npm run build:once   # Сборка CSS (один раз)
npm run preview      # Локальный просмотр
```

### Ссылки

- **Сайт на Vercel:** https://muse-liard-one.vercel.app/

---

## Подготовка к продакшену

Перед передачей программисту Битрикса:

1. **Запустить сборку:** `npm run build:once`
2. **Заменить CDN на output.css** в каждой странице
3. **Проверить работу** всех интерактивных элементов
4. **Передать документацию:** см. [docs/INTEGRATION_BITRIX.md](docs/INTEGRATION_BITRIX.md)

---

## Защита от индексации

Все страницы закрыты от индексации:
- `robots.txt` — запрещает индексацию
- `<meta name="robots" content="noindex, nofollow">` — в каждой странице

**Убрать защиту** только после полного переноса на Bitrix.

---

## Следующие шаги

1. ⏳ Переверстать оставшиеся 4 страницы
2. ⏳ Подготовить сборку для продакшена
3. ⏳ Передать программисту Битрикса
4. ⏳ Резервирование и управление архивом (обязательно)

---

## Ресурсы

- **Оригинальный сайт:** https://muse.ooo
- **Vercel:** https://muse-liard-one.vercel.app/
- **Документация:** папка `docs/`
