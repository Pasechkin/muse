# Проект Muse — Переверстка на Tailwind CSS

> **Для ИИ-агента:** Перед созданием новой страницы прочитай [AI_INSTRUCTIONS.md](AI_INSTRUCTIONS.md)

## Основная задача

Переверстать сайт **www.muse.ooo** с Bootstrap 3 (тема Stack) на Tailwind CSS.

### Ключевые требования

1. Сохранить визуальный стиль оригинала (цвета, типографика, компоновка)
2. Пошаговый подход: сначала статические страницы, потом интеграция (вне рамок этого проекта)
3. Деплой: GitHub → Vercel
4. **Безопасность публикации:** никакие действия по публикации на GitHub не должны приводить к массовой перезаписи файлов на локальном диске ПК.
5. Интеграцию будет делать программист (не в рамках этого проекта)
6. После верстки подготовить документацию/заметки для дальнейшей интеграции

---

## Текущий статус

**Дата обновления:** 30 января 2026

### Прогресс

| Категория | Готово | Всего |
|-----------|--------|-------|
| Главные страницы | 4 | 4 |
| Стили портретов | 18 | 18 |
| Объекты портретов | 5 | 5 |
| Блог | 20 | 20 |
| Info | 3 | 3 |
| **Итого (production)** | **50** | **54** |

### Последние изменения (27 января 2026)

- ✅ Info: выровнены компоненты и поведение (advantages, лого‑облако, карусели, page navigator), исправлены дубли и схема FAQ.
- ✅ Контакты: улучшена доступность табов (через `el-tab-group`).
- ✅ Добавлен минимальный стандарт форматирования через .editorconfig.
- ✅ Зафиксирован рефакторинг на страницах стилей портретов (эталон: `portret-maslom`) и на ключевых страницах:
    - `src/html/portret-na-zakaz/style/*`
    - `src/html/foto-na-kholste-sankt-peterburg.html`
    - `src/html/pechat-na-kholste-sankt-peterburg.html`
    - `src/html/portret-na-zakaz-po-foto-na-kholste-sankt-peterburg.html`
    - `src/html/index.html`
- ✅ Дизайн‑система: добавлен компонент Page Header (серый фон + крошки + H1).
- ✅ Дизайн‑система: зафиксированы компоненты (карусели, вкладки, характеристики, слайдер До/После, шаги, CTA, лого‑облако, video cover, accordion) + галерея характеристик на Tailwind Plus Elements.

### Что готово

- ✅ **50 страниц (production)** полностью переверстаны на Tailwind CSS
- ✅ **Дизайн-система v2** — переезд на Tailwind v4 и `@layer components`
- ✅ **Навигация по секциям** — используем `page-navigator`
- ✅ **Оптимизация (DRY)** — вынос повторяющихся стилей из HTML в `input.css`
- ✅ **Скрипты** — `js/nav.js` содержит общую логику (page navigator, back to top, video cover, carousel)

### Эталоны по группам страниц

- **Главные страницы** — эталон: [src/html/index.html](src/html/index.html)
- **Стили портретов** — эталон: [src/html/portret-na-zakaz/style/portret-maslom.html](src/html/portret-na-zakaz/style/portret-maslom.html)
- **Объекты портретов** — эталон: [src/html/portret-na-zakaz/object/muzhskoy-portret.html](src/html/portret-na-zakaz/object/muzhskoy-portret.html)
- **Блог** — эталон: [src/html/blog/kollazh-i-fotokollazh.html](src/html/blog/kollazh-i-fotokollazh.html)
- **Info** — эталон: [src/html/info/kontakty.html](src/html/info/kontakty.html)
- **Остальные страницы** — ориентир: [src/html/index.html](src/html/index.html) с учётом уникальности

### В работе (Рефакторинг)

- 🔄 **Чистка HTML** — удаление лишнего кода и замена навигации во всех production HTML
- 🔄 **Компонентный рефакторинг (DRY)** — унификация повторяющихся элементов на 18 стилях портретов
- 🔄 **Проверка эталона (portret-maslom)** — контейнеры/преимущества/видео/навигация проверены, кнопки и viewBox частично
- ⏳ **Масштабирование** — применить изменения на 17 остальных страницах стилей
- ⏳ **Быстрый деплой** — статус неясен, требуется диагностика
- ⏳ **4 страницы** — региональные (Москва), доставка, оплата
- 🔄 **Рефакторинг блога** — в процессе
- 🔄 **Рефакторинг объектов портретов** — в процессе
- ⏳ **Иконки навигации** — стратегия зафиксирована: только inline SVG (без SVG‑спрайта). Осталось проверить/дочистить по всем страницам и убрать блокирующие ресурсы.

### Заметка: мигание страницы (этап оптимизации кнопок)

- На этапе завершения работы с кнопками выявили мигание на странице с видео: [src/html/portret-na-zakaz/style/portret-maslom.html](src/html/portret-na-zakaz/style/portret-maslom.html)
- Причина: видео-iframe в DOM и скрытие через `display: none` провоцировали перерасчет и FOUC в Chrome
- Решение: создавать iframe только по клику + фиксированный размер контейнера + скрытие обложки через `opacity`
- Реализовано на эталоне: [src/html/portret-na-zakaz/style/portret-maslom.html](src/html/portret-na-zakaz/style/portret-maslom.html) + [src/html/js/nav.js](src/html/js/nav.js)
- Требуется внедрить решение на всех страницах, где есть видео
- Можно улучшить: skeleton-плейсхолдер, `preconnect` к YouTube, облегченный embed (lite)

#### 📋 Чек-лист компонентного рефакторинга

**Эталон:** `src/html/portret-na-zakaz/style/portret-maslom.html`

| Этап | Задача | Статус |
|------|--------|--------|
| **0. Иконки** | Оптимизация SVG-иконок: только inline SVG, убрать блокирующие ресурсы. | ⏳ Ждет |
| **1. Контейнеры** | Исправить `.container` в `input.css` (добавить `max-width: 1170px`). Убрать 11 двойных оберток в HTML. | ✅ Эталон, ⏳ Масштабирование |
| **2. Кнопки** | Разрешены компонентные классы `btn-*` из `input.css`. Заменить длинные классы в HTML. | 🔄 Частично (контент), ⏳ Header/Mobile |
| **3. Проверка 0–1** | Проверить реализацию пунктов 0 и 1 на эталоне и в `input.css`. | 🔄 Контейнеры ок, ⏳ Иконки |
| **4. Как заказать** | Внедрить `.step-container`, `.process-step`. Чистим разметку шагов и линию между ними. | ⏳ Ждет |
| **5. Преимущества** | Создать `.feature-card`. Убрать дублирование 6 карточек (15 строк → 7 строк каждая). | ✅ Эталон, ⏳ Масштабирование |
| **6. Характеристики** | Внедрить `.check-list`. Заменить SVG-галочки в HTML на CSS-символ. | ⏳ Ждет |
| **7. Навигация** | Проверить `page-navigator` на разных разрешениях экрана. | ✅ Эталон (старый), ⏳ Масштабирование |
| **8. Масштабирование** | Применить изменения к 17 другим стилям в `portret-na-zakaz/style/`. | ⏳ Ждет |

**Обнаруженные проблемы:**
- 🔴 79% секций имеют двойные контейнеры (`mx-auto px-4 max-w-[1170px]` + `.container`)
- 🔴 Кнопки дублируют ~120 символов классов вместо 1 компонента
- 🔴 Карточки преимуществ: 6×15 строк кода можно сократить до 6×7 строк

**Ожидаемый результат:**
- Размер HTML: ~1015 строк → ~600 строк (-40%)
- Поддержка: изменения в 1 файле вместо 18
- Консистентность: единый стиль на всех страницах

---

**Полный список:** см. [docs/PAGES_LIST.md](docs/PAGES_LIST.md)

### Цели рефакторинга

Основная цель рефакторинга — переход к **компонентному подходу** и обеспечение чистоты кода.

-   **Источник:** [Styling with utility classes (Tailwind Docs)](https://tailwindcss.com/docs/styling-with-utility-classes#overview)
-   **Референс:** Принципы модульности из `catalyst-ui-kit`.
-   **Стек:** Проект использует только **HTML, CSS и JavaScript**, без фреймворков типа React.

---

## Структура проекта

```
tailwind-project/
├── src/
│   ├── input.css              # Исходный CSS для Tailwind
│   ├── css/output.css          # Результат сборки PostCSS (из src/input.css)
│   └── html/                   # HTML страницы (production + _drafts)
│       ├── css/output.css      # Копия для предпросмотра (npm run copy-css)
│       ├── js/nav.js           # Скрипты для навигации (`page-navigator`) и UI
│       └── ... (папки страниц)
├── docs/                      # Документация проекта
└── package.json               # Зависимости
```

---

## Технические особенности

### Единый источник предпросмотра (Важно)

- **Единый источник**: Live Server
- **Открывать** страницу по пути: `src/html/…` (пример: `src/html/portret-na-zakaz/style/portret-maslom.html`)
- **Не использовать** альтернативные локальные сервера/превью (например, `http://127.0.0.1:3000/...`), так как там может быть другая сборка и некорректные стили (иконки чёрные и т.п.)
- Если Live Server запущен **из корня проекта**, URL будет вида: `http://127.0.0.1:5501/src/html/...`
- Если хотите короткие URL вида `http://127.0.0.1:5501/имя-страницы.html`, запускайте Live Server **из папки** `src/html`.

### CSS

- **Tailwind v4:** локальная сборка (без CDN)
- **Исходный файл:** `src/input.css` (директивы `@tailwind` + кастомные стили)
- **Конфигурация:** цвета/шрифты/контейнер задаются напрямую в `src/input.css` через `@theme` и `@layer` (архитектура Tailwind v4)
- **Результат сборки:** `src/css/output.css` (генерируется из `src/input.css`)
- **Для предпросмотра (Live Server):** `src/html/css/output.css` (обновляется командой `npm run copy-css`)
- **CDN Tailwind:** не используется (допускается только в `src/html/_drafts/` для demo/экспериментов)

### Скрипты

- **Общие функции:** `js/nav.js` (Page Navigator, Back to Top, Video Cover, Carousel Scroll и др.)
- **Inline-скрипты:** допускаются только для уникальной логики одной страницы (без дублирования того, что уже есть в `js/nav.js`)
- **Tailwind Plus Elements:** локальный вендор, подключать только если на странице используются компоненты `el-*` (мобильное меню, табы, аккордеоны)
- **Правило:** `tailwindplus-elements.js` не удалять без согласования — скрипт может быть нужен для меню даже при отсутствии `el-*` в текущей разметке.
- **Tailwind Plus Elements (версия/дата):** `@tailwindplus/elements` v1.0.22 (установлено 28 января 2026). Подключение — локальный файл `src/html/js/tailwindplus-elements.js`.
- **Важно:** на отдельных страницах временно добавлены fallback‑скрипты (табы/аккордеоны) — удалить после стабилизации Tailwind Plus. См. трекер ниже.

#### Трекер временных fallback (к удалению)

| Что | Где (страница) | Причина | Где реализовано | Условие удаления | Статус |
|-----|---------------|---------|-----------------|------------------|--------|
| Tabs fallback | ~~`src/html/portret-na-zakaz/style/drim-art-portret.html`~~, `src/html/portret-na-zakaz/style/beauty-art-portret.html` | Стабилизация `el-tab-group` (вкладки/галерея характеристик) | Page-specific inline `ensureTabsFallback` | Когда вкладки работают через `el-tab-group` без fallback | 🔄 drim-art удалён (30.01.2026) |
| Accordion fallback | `src/html/foto-na-kholste-sankt-peterburg.html`, `src/html/pechat-na-kholste-sankt-peterburg.html`, `src/html/portret-na-zakaz-po-foto-na-kholste-sankt-peterburg.html`, `src/html/info/faq.html` | Fallback для аккордеона, если `el-disclosure` не зарегистрирован (обработка `command/commandfor` + `hidden`) | Page-specific inline: обработчик кликов по `[command][commandfor]` (guard: `customElements.get("el-disclosure")`) | Когда аккордеоны работают через `el-disclosure` без fallback или переведены на `<details>` по канону | ⏳ |
- **Основной файл:** `src/html/js/nav.js`. Содержит общую логику для интерактивных компонентов:
    - Навигация по секциям (`page-navigator`)
    - Кнопка "Наверх" (Back to Top)
    - Video Cover (ленивое создание iframe)
    - Carousel Scroll
- **Сторонние скрипты:** Tailwind Plus Elements подключён локально.
- **План рефакторинга:** Перенести остальные сторонние скрипты с CDN на локальную установку через `npm` для финальной сборки.

---

## Виджет мессенджеров (Floating Messenger Widget)

Назначение: плавающий виджет для ненавязчивого доступа к мессенджерам, появляется с задержкой после загрузки страницы, раскрывается по клику и закрывается при клике вне области или при скролле.

**Файлы:**
- Разметка/пример: [src/html/_drafts/widget.html](src/html/_drafts/widget.html)
- Иконки (канон мобильного меню, цветные 24px): [src/html/social-icons-demo.html](src/html/social-icons-demo.html)

**Поведение:**
1. Отложенное появление (10 секунд после загрузки).
2. Открытие/закрытие по клику на кнопку.
3. Автозакрытие при клике вне виджета и при скролле.
4. Состояние управляется через `aria-expanded` и класс `is-open` у списка.

**Размеры (канон для виджета):**
- Кнопка: 48×48 (w-12 h-12)
- Иконка: 24×24 (w-6 h-6)

**CSS:**
- Компонентные классы в [src/input.css](src/input.css) (`messenger-widget`, `messenger-list`, `messenger-list.is-open`).
- Для предпросмотра необходима сборка CSS и копия в [src/html/css/output.css](src/html/css/output.css).

### Брейкпоинты (стандартные Tailwind)

| Класс | Ширина | Использование |
|-------|--------|---------------|
| `sm` | 640px | Мобильные (горизонтально) |
| `md` | 768px | Планшеты |
| `lg` | 1024px | Ноутбуки |
| `xl` | 1280px | Большие мониторы |

### Контейнер

- Максимальная ширина: **1170px**
- Padding: **16px** с каждой стороны
- Класс: `container`

**Правило:** в секции допускается только один `container`, без двойных оберток.
**Исключения:** полноширинные секции с фоном — Header, Footer, CTA, Промо‑баннер (Акция), Карусели. Там используется один `container` только для контента.

---

## Резервирование и публикация

### Резервирование

Основная система резервирования — **Git + GitHub**.

- Регулярные коммиты — обязательны после завершения задач.
- Удалены локальные архивные папки (archive_legacy/ и backup_legacy/ не используются).
- История изменений хранится в git‑репозитории.

### Публикация на GitHub

Используйте скрипт:

- `upload-to-github.bat` — инициализация репозитория (если нужно), коммит и push в GitHub.

### Публикация на Vercel

Используйте быстрый деплой:

- `БЫСТРЫЙ_ДЕПЛОЙ.bat` или `БЫСТРЫЙ_ДЕПЛОЙ.ps1` — сборка CSS, коммит и push.
- После push Vercel запускает деплой автоматически.

#### Production Branch = main

**Production-ветка:** `main` — единственный источник правды для Vercel.

Настройки Vercel:
1. Vercel → Project → **Settings** → **Git**
2. **Production Branch** → `main`

Если на production URL видна "старая версия", проверьте:
- Пуш был в `main`?
- Production Branch в Vercel = `main`?
- При необходимости: **Deployments** → **Redeploy** (с очисткой build cache)

---

## Деплой

### Быстрый способ

Запустить **`БЫСТРЫЙ_ДЕПЛОЙ.bat`** — он сделает всё автоматически.

### Команды

```bash
npm install          # Установка зависимостей
npm run build        # Сборка CSS (с отслеживанием)
npm run build:once   # Сборка CSS (один раз)
npm run preview      # Локальный просмотр (не эталон; эталон — Live Server)
```

### Ссылки

- **Сайт на Vercel:** https://muse-liard-one.vercel.app/
- **Ссылки в тексте (утверждено):** подчёркивание по умолчанию — `text-primary underline hover:no-underline` (см. `AI_INSTRUCTIONS.md`).

---

## Подготовка к продакшену

Перед передачей проекта в интеграцию:

1. **Запустить сборку:** `npm run build:once`
2. **Обновить копию для HTML-предпросмотра (если нужно):** `npm run copy-css`
3. **Заменить CDN на output.css** в каждой странице
4. **Проверить работу** всех интерактивных элементов
5. **Передать документацию:** см. [docs/INTEGRATION.md](docs/INTEGRATION.md)

---

## Защита от индексации

Все страницы закрыты от индексации:
- `robots.txt` — запрещает индексацию
- `<meta name="robots" content="noindex, nofollow">` — в каждой странице

**Убрать защиту** только после полного переноса на production.

---

## Следующие шаги

1. ⏳ Переверстать оставшиеся 4 страницы
2. ⏳ Подготовить сборку для продакшена
3. ⏳ Подготовить материалы для интеграции
4. ⏳ Резервирование и управление архивом (обязательно)
5. ⏳ Сверстать новое меню и подвал
6. ✅ Утвердить и внедрить правило по ссылкам внутри текстов
7. ⏳ Проверить читаемость текста и контраста на всех страницах (gray-400/500 → при необходимости затемнить до gray-600)
8. ⏳ Проверить скрипты, стили и critical path постранично
9. ✅ Утвердить базовые правила для изображений (атрибуты, loading/decoding, LCP/preload)
10. ⏳ По каждому видео обсудить загрузку (удобство/производительность)
11. ⏳ Предусмотреть верстку калькуляторов
12. ⏳ Постраничное сравнение с muse.ooo (SEO‑тексты, ссылки, изображения, поведение) с фиксацией в комментариях страниц

Для постраничного аудита соответствия правилам см. [docs/AUDIT_PAGE_COMPLIANCE.md](docs/AUDIT_PAGE_COMPLIANCE.md).

---

## План рефакторинга (из docs/Refactorig.txt)

**Эталон:** [src/html/portret-na-zakaz/style/portret-maslom.html](src/html/portret-na-zakaz/style/portret-maslom.html)
**Дата эталона:** 25.01.2026

### Обязательные правила
1) Не менять контент (тексты/alt/title/ссылки/порядок секций).
2) Header/Footer не менять без отдельного согласования.
3) Изменения в `input.css` требуют пересборки: `npm run build:once` + copy CSS.
4) Проверка результата только через Live Server (src/html/…).
5) Все правки сначала на эталоне, затем масштабирование.
6) `main` не менять без согласования.

### Этапы и статус (предварительно)

| Этап | Цель | Статус | Примечание |
|---|---|---|---|
| 0.1 Санитарные правила | Удалить header/footer, CDN Tailwind, дубли | ⏳ | Требует массовой проверки по всем страницам |
| 0. Иконки | Убрать блокирующие ресурсы, inline SVG | ⏳ | Нужна проверка всех секций |
| 1. Контейнеры | `.container` 1170px + убрать двойные | ✅/⏳ | CSS ок: [src/input.css](src/input.css#L120-L131), HTML — проверить |
| 2. Кнопки | Компоненты вместо длинных классов | ✅/🔄 | CSS ок: [src/input.css](src/input.css#L151-L184), контент частично |
| 3. Проверка 0–1 | Зафиксировать итог на эталоне | 🔄 | Иконки ещё не подтверждены |
| 4. Как заказать | `step-container`/`process-step` | ✅ | В эталоне: [portret-maslom.html](src/html/portret-na-zakaz/style/portret-maslom.html#L229) |
| 5. Преимущества | `advantages-*` + inline SVG | ✅/⏳ | В эталоне есть, масштабирование — проверить |
| 6. Характеристики | `check-list`, убрать SVG‑галочки | ✅ | В эталоне: [portret-maslom.html](src/html/portret-na-zakaz/style/portret-maslom.html#L313-L339) |
| 7. Навигация | Старый `page-navigator` | ✅ | CSS/JS есть: [src/input.css](src/input.css#L137-L147), [src/html/js/nav.js](src/html/js/nav.js#L14-L61) |
| 8. Масштабирование | 17 остальных стилей | ✅/🔎 | По списку Refactorig — все 18, нужна выборочная проверка |
| ViewBox | Оптимизировать большой viewBox | 🔎 | В эталоне 4500x4500 не найдено |
| Видео | Iframe только по клику | ✅ | [src/html/js/nav.js](src/html/js/nav.js#L63-L110) |
| Быстрый деплой | Проверить батники/ps1 | ⏳ | Требует запуска и проверки логов |

### Компоненты
- **Промо‑баннер (акция)** — фиксированные `bg-primary` + `py-6`, текст из CMS не менять.
- **FAQ‑аккордеон** — `<details>/<summary>` со schema.org, без JS (исключения по согласованию).
- **Карусели** — единая база `.carousel-scroll` + модификаторы.

### Стили портретов (масштабирование)
Список 18/18 по Refactorig: все файлы в [src/html/portret-na-zakaz/style/](src/html/portret-na-zakaz/style/) отмечены как готовые. Требуется выборочная перепроверка.

---

## Ресурсы

- **Оригинальный сайт:** https://muse.ooo
- **Vercel:** https://muse-liard-one.vercel.app/
- **Документация:** папка `docs/`
