# 📤 Как загрузить проект в GitHub (БЕЗ node_modules)

## ✅ Важно: node_modules НЕ будет загружена!

Файл `.gitignore` уже настроен - папка `node_modules` автоматически исключена из загрузки.

## 📝 Пошаговая инструкция:

### Шаг 1: Создайте репозиторий на GitHub

1. Зайдите на [github.com](https://github.com)
2. Войдите в аккаунт (или создайте новый)
3. Нажмите кнопку **"+"** в правом верхнем углу → **"New repository"**
4. Назовите репозиторий, например: `muse-site`
5. Выберите **"Public"** (публичный)
6. **НЕ** ставьте галочки на "Add README", "Add .gitignore" и "Choose a license" (у нас уже всё есть)
7. Нажмите **"Create repository"**

### Шаг 2: Загрузите файлы

#### Вариант А: Через GitHub Desktop (самый простой)

1. Скачайте [GitHub Desktop](https://desktop.github.com/)
2. Установите и войдите
3. File → Add Local Repository
4. Выберите папку `tailwind-project`
5. Внизу напишите коммит: "Initial commit"
6. Нажмите "Commit to main"
7. Нажмите "Publish repository"
8. Выберите репозиторий, который создали
9. Готово!

#### Вариант Б: Через веб-интерфейс GitHub

1. На странице созданного репозитория найдите кнопку **"uploading an existing file"**
2. Перетащите туда **ВСЮ папку** `tailwind-project` (кроме node_modules - она автоматически не загрузится)
3. Внизу напишите: "Initial commit"
4. Нажмите **"Commit changes"**

#### Вариант В: Через командную строку (для продвинутых)

```bash
cd tailwind-project
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/ВАШ_ЛОГИН/muse-site.git
git push -u origin main
```

## ✅ Что будет загружено:

- ✅ Все HTML файлы
- ✅ CSS файлы (исходные)
- ✅ Конфигурационные файлы
- ✅ README и инструкции

## ❌ Что НЕ будет загружено (благодаря .gitignore):

- ❌ node_modules (очень большая папка)
- ❌ package-lock.json
- ❌ Скомпилированные CSS файлы (output.css)

## 🔄 После загрузки:

Когда кто-то склонирует ваш репозиторий, он просто выполнит:
```bash
npm install
```
И всё установится автоматически!

## 💡 Проверка:

После загрузки зайдите на GitHub и проверьте - папки `node_modules` там быть не должно!


