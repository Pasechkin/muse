# Студия Muse - Статический сайт на Tailwind CSS

Современный адаптивный статический сайт для студии Muse с использованием Tailwind CSS.

## 🚀 Быстрый старт

### Установка зависимостей

```bash
npm install
```

### Локальная разработка

```bash
# Сборка CSS с отслеживанием изменений
npm run build

# Или сборка один раз
npm run build:once

# Копирование CSS в папку html
npm run copy-css

# Просмотр в браузере (после сборки)
npm run preview

# Просмотр с доступом по сети (для мобильных)
npm run preview:mobile
```

## 📁 Структура проекта

```
tailwind-project/
├── src/
│   ├── html/              # HTML файлы статического сайта
│   │   ├── index.html     # Главная страница
│   │   ├── css/           # Скомпилированный CSS
│   │   │   └── output.css
│   │   ├── portret-na-zakaz/  # Страница портретов
│   │   │   └── index.html
│   │   └── ...
│   ├── css/               # Исходный CSS (для разработки)
│   │   └── output.css
│   └── input.css          # Исходный CSS с Tailwind
├── tailwind.config.js     # Конфигурация Tailwind
├── vercel.json            # Конфигурация для Vercel
├── netlify.toml           # Конфигурация для Netlify
├── .github/workflows/     # GitHub Actions для автодеплоя
└── package.json
```

## 🌐 Деплой

### Vercel (рекомендуется)

1. Загрузите код на GitHub (см. GITHUB_SETUP.md)
2. Зайдите на [vercel.com](https://vercel.com)
3. Подключите репозиторий
4. Настройки автоматически определяются из `vercel.json`:
   - Build Command: `npm run build:once && npm run copy-css`
   - Output Directory: `src/html`
5. Нажмите Deploy

### Netlify

1. Загрузите код на GitHub
2. Зайдите на [netlify.com](https://netlify.com)
3. Import an existing project
4. Выберите репозиторий
5. Настройки из `netlify.toml`:
   - Build command: `npm run build:once && npm run copy-css`
   - Publish directory: `src/html`

### GitHub Pages

1. После загрузки на GitHub, настройки автоматические через GitHub Actions
2. Settings → Pages → выбирайте источник деплоя
3. Сайт будет доступен по адресу: `https://ВАШ_ЛОГИН.github.io/muse-site/`

## 📱 Тестирование на мобильном устройстве

### Способ 1: Через локальную сеть

1. Убедитесь, что ноутбук и телефон в одной Wi-Fi сети
2. Запустите: `npm run preview:mobile`
3. Узнайте IP адрес ноутбука: `ipconfig` (Windows)
4. На телефоне откройте: `http://ВАШ_IP:3000`

### Способ 2: Через ngrok

1. Установите ngrok: https://ngrok.com/download
2. Запустите: `npm run preview`
3. В другом терминале: `ngrok http 3000`
4. Используйте публичный URL из ngrok

## 🔧 Технологии

- **Tailwind CSS 4** - утилитарный CSS фреймворк
- **PostCSS** - обработка CSS
- **GitHub Actions** - автоматический деплой
- **Vercel/Netlify** - хостинг статических сайтов

## 📝 Полезные команды

```bash
# Разработка с автообновлением CSS
npm run build

# Сборка один раз
npm run build:once

# Копирование CSS в папку html
npm run copy-css

# Локальный просмотр
npm run preview

# Просмотр с доступом по сети
npm run preview:mobile
```

## 📚 Дополнительная документация

- `GITHUB_SETUP.md` - инструкция по загрузке на GitHub
- `КАК_РАБОТАТЬ_ЛОКАЛЬНО.md` - работа локально
- `DEPLOY.md` - подробности о деплое

## ✅ Что готово

- ✅ Адаптивный дизайн
- ✅ Главная страница с каруселями
- ✅ Страница портретов
- ✅ Страница контактов
- ✅ Автоматический деплой настроен
- ✅ Готово к публикации
